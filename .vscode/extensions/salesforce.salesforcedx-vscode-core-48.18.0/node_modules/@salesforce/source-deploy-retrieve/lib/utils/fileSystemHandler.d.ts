import { SourcePath } from '../types';
export declare function ensureDirectoryExists(filePath: string): void;
/**
 * Method to save multiple files on disk.
 *
 * @param fileMap key = filePath, value = file contents
 */
export declare function createFiles(fileMap: Map<string, string>): void;
export declare function isDirectory(fsPath: SourcePath): boolean;
/**
 * Traverse up a file path and search for the given file name.
 *
 * @param start File or folder path to start searching from
 * @param fileName File name to search for
 */
export declare function searchUp(start: SourcePath, fileName: string): string | undefined;
/**
 * Walk a given directory path and collect the files.
 *
 * @param dir Directory to walk
 * @param ignore Optional paths to ignore
 */
export declare function walk(dir: SourcePath, ignore?: Set<SourcePath>): SourcePath[];
