import { AuraDefinition } from '../../utils/deploy';
import { MetadataComponent, DeployResult, SourceResult } from '../../types';
import { BaseDeploy } from './baseDeploy';
export declare class AuraDeploy extends BaseDeploy {
    deploy(component: MetadataComponent): Promise<DeployResult>;
    buildDefList(): Promise<AuraDefinition[]>;
    upsert(auraDef: AuraDefinition): Promise<SourceResult>;
    private getAuraFormat;
    private getAuraDefType;
    private findAuraDefinitions;
    private parseAuraError;
}
