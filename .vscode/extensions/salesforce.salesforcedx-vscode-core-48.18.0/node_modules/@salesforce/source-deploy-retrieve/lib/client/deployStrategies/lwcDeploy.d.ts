import { LightningComponentResource } from '../../utils/deploy';
import { MetadataComponent, DeployResult, SourceResult } from '../../types';
import { BaseDeploy } from './baseDeploy';
export declare class LwcDeploy extends BaseDeploy {
    deploy(component: MetadataComponent): Promise<DeployResult>;
    buildResourceList(): Promise<LightningComponentResource[]>;
    upsert(lightningResources: LightningComponentResource[]): Promise<SourceResult[]>;
    private findLightningResources;
    private parseLwcError;
}
