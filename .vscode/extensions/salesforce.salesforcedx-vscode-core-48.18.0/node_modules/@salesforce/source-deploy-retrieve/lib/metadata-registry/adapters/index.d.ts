import { SourceAdapter, MetadataType } from '../../types';
import { ForceIgnore } from '../forceIgnore';
export declare enum AdapterId {
    Bundle = "bundle",
    MatchingContentFile = "matchingContentFile",
    MixedContent = "mixedContent"
}
export declare const getAdapter: (type: MetadataType, adapterId: AdapterId, forceIgnore?: ForceIgnore) => SourceAdapter;
